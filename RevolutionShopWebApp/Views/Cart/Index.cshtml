@using RevolutionData.Models.Entities
@*@model List<Cart>*@
@{
    ViewBag.Title = "Корзина";
}

<!DOCTYPE html>
<html>
	<body>
    @if(ViewBag.Cart != null)
    {
        <p>@ViewBag.Cart.Count</p>
        <table>
        @for(var i = 0; i < ViewBag.Cart.Count; i++)
        {
            <tr>
                <td>Футболка @ViewBag.Cart[i].TShirt.Name</td>
                <td>Цена @ViewBag.Cart[i].TShirt.Price</td>
                <td>Количество @ViewBag.Cart[i].Quantity</td>
            </tr>
        }
        </table>
    }
    else
    {
        <p>Корзина пуста</p>
    }
    <table>
        @*@foreach (var item in ViewBag.Cart)
        {
            <tr>
                <td>@item.</td>
                <td>@item.Product.Name</td>
                <td>@item.Product.Price</td>
                <td>@item.Quantity</td>
            </tr>
        }*@
        </table>
		@*<div>
        @if(Model.Count > 0)
        {
            <div>
                <h1>Корзина</h1>
                <table>
                    @for (var i = 0; i < Model.Count; i++)
                    {
                        <tr>
                            <td>
                                <p>@Model[i].ProductName</p>
                                <p>@Model[i].ProductPrice</p>
                                <p>Количество @Model[i].Quantity</p>
                            </td>
                        </tr>
                    }
                </table>
            </div>
            <div>
                <h1>Оформление заказа</h1>
                <button onclick="location.href='@Url.Action("CreateOrder", "Cart")'">Оформить заказ</button>
            </div>
        }
        else
        {
            <p>Корзина пуста</p>
        }
        </div>*@
	</body>
</html>